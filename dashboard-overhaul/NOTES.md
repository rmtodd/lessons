# Teaching notes

- Raymond is new to software development. Assume no prior knowledge of SQL, APIs, git, or web frameworks — define every term on first use.
- Explicit preference (2026-07-03): lessons should be **dense, not brief**. Do not trim detail for the sake of a quick read. This overrides the teach skill's general "keep lessons short" default for this workspace.
- The mission tracks a real, already-partially-executed plan (`~/.claude/plans/you-are-fable-the-piped-stallman.md`). Phases 1-4 were done before teaching started; treat those as "here's what already happened and why," not hypothetical future work.
- One lesson per **plan phase** (Phase 1 through Phase 9, including the inserted Phase 6.5), not per abstract CS topic — this matches how Raymond will track the project's actual progress log.
- Reuse `assets/style.css` and `reference/glossary.html` across all lessons so terminology stays consistent — Raymond will be relearning the same handful of core concepts (migration, RLS, RPC, trigger, ledger, actor/JWT auth) in every phase.
